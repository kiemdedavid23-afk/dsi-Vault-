<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet : uniquement pour la vérification de mise à jour (Partie 1 §6) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Android 13+ (API 33) : permissions granulaires par type de média -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Android < 13 : permission historique (dépréciée progressivement) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:supportsPictureInPicture="true">
            <!--
              supportsPictureInPicture active le PiP natif (Partie 4 §18, Partie 7 §13).
              Déclenchement réel : MainActivity.enterPipModeIfSupported(), appelé via
              PipModule.kt depuis VideoPlayerScreen.tsx.
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Réception de fichiers depuis d'autres applications (Partie 2 §26) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="video/*" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!--
          ⚠️ NON VÉRIFIÉ (à confirmer après `npm install` réel) : le nom de classe exact
          du service de premier plan fourni par react-native-track-player dépend de sa
          version. Habituellement autolinké automatiquement (le module ajoute lui-même
          son <service> dans son propre AndroidManifest.xml, fusionné au build) — cette
          déclaration manuelle n'est PAS certaine d'être nécessaire ni exacte. Si le
          build échoue sur un service dupliqué ou introuvable, retirer ce bloc et
          laisser l'autolinking du module gérer seul cette entrée.
        -->
        <!--
        <service
            android:name="com.doublesymmetry.trackplayer.service.MusicService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />
        -->

    </application>
</manifest>
